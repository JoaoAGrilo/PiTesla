import React from 'react'
import { SafeAreaView } from 'react-native'
import RedeemCoins from '../../components/organisms/students/RedeemCoins'

export default () => {
    return (
        <SafeAreaView style={{flex: 1, backgroundColor: '#006BFF'}}>
            <RedeemCoins/>
        </SafeAreaView>
    )
}